//#pragma once

//enum flags {Z_FLAG, N_FLAG, H_FLAG, C_FLAG};
//enum reg_index {A, B, C, D, E, H, L};
//enum operators {ADD, SUB, DIV, MUL, AND, OR, XOR, NOT, INC, DEC};

extern char* registers;
extern bool* F;
